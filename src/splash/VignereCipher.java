/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package splash;

/**
 *
 * @author shagilsid
 */
public class VignereCipher {
    final static String key="PIRACYGUARDIAN";
     public static String encrypt(String text)
    {
        
        String res = "";
        text = text.toUpperCase();
        for (int i = 0, j = 0; i < text.length(); i++)
        {
            char c = text.charAt(i);
            if (c < 'A' || c > 'Z')
                continue;
            res += (char) ((c + key.charAt(j) - 2 * 'A') % 26 + 'A');
            j = ++j % key.length();
        }
        return res;
    }
 
    /*public static String decrypt(String text)
    {
        String res = "";
        text = text.toUpperCase();
        for (int i = 0, j = 0; i < text.length(); i++)
        {
            char c = text.charAt(i);
            if (c < 'A' || c > 'Z')
                continue;
            res += (char) ((c - key.charAt(j) + 26) % 26 + 'A');
            j = ++j % key.length();
        }
        return res;
    }
    /*public static void main(String[] args)
    {
        String key = "SHAGIL";
        String message = "I am awesome";
        String encryptedMsg = encrypt(message);
        System.out.println("String: " + message);
        System.out.println("Encrypted message: " + encryptedMsg);
        System.out.println("Decrypted message: " + decrypt(encryptedMs));
    }*/
    
   // FNAME,LNAME, EMAILID,CUSTOMR ID, MAC ADDRESS
     //       CUSTOMERID , SOFTWARE, SERIAL KEY
    
}
